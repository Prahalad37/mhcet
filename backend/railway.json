{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "[ \"$SKIP_DB_MIGRATE\" = \"true\" ] || npm run migrate && npm start",
    "healthcheckPath": "/health"
  }
}
